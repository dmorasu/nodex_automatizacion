var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var models_exports = {};
__export(models_exports, {
  StorageFileAudience: () => StorageFileAudience,
  fileAttributesToString: () => fileAttributesToString,
  fileChangeTimeToString: () => fileChangeTimeToString,
  fileCreationTimeToString: () => fileCreationTimeToString,
  fileLastWriteTimeToString: () => fileLastWriteTimeToString,
  getFileServiceAccountAudience: () => getFileServiceAccountAudience,
  toShareProtocols: () => toShareProtocols,
  toShareProtocolsString: () => toShareProtocolsString,
  validateAndSetDefaultsForFileAndDirectoryCreateCommonOptions: () => validateAndSetDefaultsForFileAndDirectoryCreateCommonOptions,
  validateAndSetDefaultsForFileAndDirectorySetPropertiesCommonOptions: () => validateAndSetDefaultsForFileAndDirectorySetPropertiesCommonOptions,
  validateFilePermissionOptions: () => validateFilePermissionOptions
});
module.exports = __toCommonJS(models_exports);
var import_FileSystemAttributes = require("./FileSystemAttributes.js");
var import_utils_common = require("./utils/utils.common.js");
var import_log = require("./log.js");
function toShareProtocols(protocolsString) {
  if (protocolsString === void 0) {
    return void 0;
  }
  const protocolStrArray = protocolsString.split(";");
  const protocols = {};
  for (const protocol of protocolStrArray) {
    if (protocol === "SMB") {
      protocols.smbEnabled = true;
    } else if (protocol === "NFS") {
      protocols.nfsEnabled = true;
    }
  }
  return protocols;
}
function toShareProtocolsString(protocols = {}) {
  let protocolStr = void 0;
  if (protocols.smbEnabled === true) {
    protocolStr = "SMB";
  }
  if (protocols.nfsEnabled === true) {
    import_log.logger.info(
      `Using "NFS" in favor of "SMB" for the share protocol as currently they can't be supported at the same time.`
    );
    protocolStr = "NFS";
  }
  return protocolStr;
}
function validateFilePermissionOptions(filePermission, filePermissionKey) {
  if (filePermission && filePermissionKey) {
    throw new RangeError("Only one of filePermission or filePermissionKey can be specified.");
  }
}
function validateAndSetDefaultsForFileAndDirectoryCreateCommonOptions(options) {
  if (!options) {
    options = {};
  }
  validateFilePermissionOptions(options.filePermission, options.filePermissionKey);
  return options;
}
function validateAndSetDefaultsForFileAndDirectorySetPropertiesCommonOptions(options) {
  if (!options) {
    options = {};
  }
  validateFilePermissionOptions(options.filePermission, options.filePermissionKey);
  return options;
}
function fileAttributesToString(fileAttributes) {
  return fileAttributes instanceof import_FileSystemAttributes.FileSystemAttributes ? fileAttributes.toString() : fileAttributes;
}
function fileCreationTimeToString(time) {
  return time instanceof Date ? (0, import_utils_common.truncatedISO8061Date)(time) : time;
}
function fileLastWriteTimeToString(time) {
  return time instanceof Date ? (0, import_utils_common.truncatedISO8061Date)(time) : time;
}
function fileChangeTimeToString(time) {
  return time instanceof Date ? (0, import_utils_common.truncatedISO8061Date)(time) : time;
}
var StorageFileAudience = /* @__PURE__ */ ((StorageFileAudience2) => {
  StorageFileAudience2["StorageOAuthScopes"] = "https://storage.azure.com/.default";
  return StorageFileAudience2;
})(StorageFileAudience || {});
function getFileServiceAccountAudience(storageAccountName) {
  return `https://${storageAccountName}.file.core.windows.net/.default`;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  StorageFileAudience,
  fileAttributesToString,
  fileChangeTimeToString,
  fileCreationTimeToString,
  fileLastWriteTimeToString,
  getFileServiceAccountAudience,
  toShareProtocols,
  toShareProtocolsString,
  validateAndSetDefaultsForFileAndDirectoryCreateCommonOptions,
  validateAndSetDefaultsForFileAndDirectorySetPropertiesCommonOptions,
  validateFilePermissionOptions
});
//# sourceMappingURL=models.js.map
